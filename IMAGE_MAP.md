# Extracted portfolio images

The original HTML embedded four images as Base64 data URIs. They have been extracted into the `images/` folder and the corresponding `<img src>` values in `index.html` have been replaced with relative paths.

## 1. `find-cam-app-takeover.png`
- HTML path: `images/find-cam-app-takeover.png`
- Existing alt text: Find Cam takeover shown live in the app home feed alongside game schedule and progress tracker, on two phones

## 2. `bobblehead-at-game.jpg`
- HTML path: `images/bobblehead-at-game.jpg`
- Existing alt text: Fan holding the physical Cam Jordan bobblehead at the game

## 3. `find-cam-team-page.png`
- HTML path: `images/find-cam-team-page.png`
- Existing alt text: Cam Jordan bobblehead hidden inside the Team page roster, with trending videos below it

## 4. `saints-app-rebrand-before-after.png`
- HTML path: `images/saints-app-rebrand-before-after.png`
- Existing alt text: Saints app homepage before and after the dark mode rebrand, shown side by side in phone mockups

Keep `index.html` and the `images/` folder together in the repository. For GitHub Pages, both can live at the repository root.